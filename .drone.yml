kind: pipeline
name: default

steps:
- name: test
  image: node
  commands:
  - yarn
  - yarn test:unit

- name: publish
  image: node
  commands:
  - echo publish
  when:
    branch:
    - master

- name: slack
  image: plugins/slack
  settings:
    webhook:
      from_secret: slack_webhook
    channel: builds
  when:
    status: [ success, failure ]